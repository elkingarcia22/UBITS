{
  "name": "@ubits/slider",
  "version": "1.0.0",
  "description": "Componente Slider UBITS como add-on intercambiable con orientación horizontal/vertical, modo single/range, y opción de inputs",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --declaration --skipLibCheck --noEmit false 2>&1 | grep -v '../list' | grep -v '../calendar' | grep -v '../input' | grep -v '../modal' | grep -v '../scroll' || tsc --declaration --skipLibCheck; vite build",
    "dev": "tsc --watch",
    "lint": "biome check .",
    "format": "biome format . --write"
  },
  "dependencies": {
    "@ubits/tokens": "workspace:*",
    "@ubits/icons": "workspace:*",
    "@ubits/typography": "workspace:*",
    "@ubits/input": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^7.1.12"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./styles": "./dist/slider.css"
  }
}

