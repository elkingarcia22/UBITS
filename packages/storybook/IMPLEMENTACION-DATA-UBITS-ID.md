# Implementación de data-ubits-id en Providers

Este documento rastrea el progreso de agregar `data-ubits-id` a todos los Providers de componentes UBITS.

## ✅ Completados (48/55+ - 87%+)

1. **Button** (`🧩-ux-button`) ✅
2. **Badge** (`🧩-ux-badge`) ✅
3. **Input** (`🧩-ux-input`) ✅
4. **DataView** (`🧩-ux-data-view`) ✅
5. **Alert** (`🧩-ux-alert`) ✅
6. **Avatar** (`🧩-ux-avatar`) ✅
7. **Breadcrumb** (`🧩-ux-breadcrumb`) ✅
8. **Chip** (`🧩-ux-chip`) ✅
9. **Checkbox** (`🧩-ux-checkbox`) ✅
10. **RadioButton** (`🧩-ux-radio-button`) ✅
11. **Toggle** (`🧩-ux-toggle`) ✅
12. **SimpleCard** (`🧩-ux-simple-card`) ✅
13. **CardContent** (`🧩-ux-card-content`) ✅
14. **List** (`🧩-ux-list`) ✅
15. **Sidebar** (`🧩-ux-sidebar`) ✅
16. **TabBar** (`🧩-ux-tabbar`) ✅
17. **DataTable** (`🧩-ux-data-table`) ✅
18. **Modal** (`⚙️-functional-modal`) ✅
19. **Drawer** (`⚙️-functional-drawer`) ✅
20. **Toast** (`🧩-ux-toast`) ✅
21. **Tooltip** (`🧩-ux-tooltip`) ✅
22. **Popover** (`🧩-ux-popover`) ✅
23. **ProgressBar** (`🧩-ux-progress-bar`) ✅
24. **EmptyState** (`🧩-ux-empty-state`) ✅
25. **Skeleton** (`🧩-ux-skeleton`) ✅
26. **Spinner** (`🧩-ux-spinner`) ✅
27. **StatusTag** (`🧩-ux-status-tag`) ✅
28. **Pagination** (`🧩-ux-pagination`) ✅
29. **SearchButton** (`🧩-ux-search-button`) ✅
30. **Accordion** (`🧩-ux-accordion`) ✅
31. **Tabs** (`🧩-ux-tabs`) ✅
32. **ButtonAI** (`🧩-ux-button-ai`) ✅
33. **ButtonFeedback** (`🧩-ux-button-feedback`) ✅
34. **Calendar** (`🧩-ux-calendar`) ✅
35. **Carousel** (`🧩-ux-carousel`) ✅
36. **FileUpload** (`🧩-ux-file-upload`) ✅
37. **Gallery** (`🧩-ux-gallery`) ✅
38. **HeaderSection** (`🧩-ux-header-section`) ✅
39. **Mask** (`🧩-ux-mask`) ✅
40. **Menu** (`🧩-ux-menu`) ✅
41. **ParticipantsMenu** (`🧩-ux-participants-menu`) ✅
42. **ProgressGeneralCard** (`🧩-ux-progress-general-card`) ✅
43. **ScoreCardMetrics** (`🧩-ux-score-card-metrics`) ✅
44. **Stepper** (`🧩-ux-stepper`) ✅
45. **Timeline** (`🧩-ux-timeline`) ✅
46. **TreeMenu** (`🧩-ux-tree-menu`) ✅
47. **Scroll** (`⚙️-functional-scroll`) ✅
48. **StatsCard** (`🧩-ux-stats-card`) ✅

## 📋 Componentes Adicionales con data-ubits-id

Los siguientes componentes también tienen `data-ubits-id` implementado:
- BarMetricCard (`🧩-ux-bar-metric-card`) ✅
- CSATMetricCard (`🧩-ux-csat-metric-card`) ✅
- NPSCard (`🧩-ux-nps-card`) ✅
- MetricCard (`🧩-ux-metric-card`) ✅
- SegmentControl (`🧩-ux-segment-control`) ✅
- SelectionCard (`🧩-ux-selection-card`) ✅
- Slider (`🧩-ux-slider`) ✅
- SubNav (`🧩-ux-subnav`) ✅

## ✅ Estado Final

**Todos los Providers TypeScript principales tienen `data-ubits-id` implementado.**

- ✅ 48+ componentes listados explícitamente
- ✅ 8+ componentes adicionales verificados
- ✅ Total: **56+ componentes completados**

**Nota:** Los archivos `.js` encontrados son versiones legacy o compiladas. Los archivos fuente `.ts` están todos actualizados.

## 🔧 Patrón de Implementación

### Para funciones `render*` (retornan HTML string):

```typescript
// Antes:
return `<div class="${classes}">${content}</div>`;

// Después:
return `<div class="${classes}" data-ubits-id="🧩-ux-component">${content}</div>`;
```

### Para funciones `create*` (retornan HTMLElement):

```typescript
// Antes:
const element = document.createElement('div');
element.className = classes;
return element;

// Después:
const element = document.createElement('div');
element.className = classes;
element.setAttribute('data-ubits-id', '🧩-ux-component');
return element;
```

### Casos especiales:

1. **Si el elemento root puede variar** (ej: Badge puede retornar `<span>` o `<div>`):
   - Agregar `data-ubits-id` a todos los posibles elementos root

2. **Si hay wrapper externo** (ej: Input con atributos):
   - Agregar `data-ubits-id` al wrapper externo

3. **Si se clona el elemento** (ej: Badge):
   - Verificar y agregar `data-ubits-id` después de clonar

4. **Componentes con overlay** (Modal, Drawer):
   - Agregar `data-ubits-id` al elemento interno (`.ubits-modal`, `.ubits-drawer`), no al overlay

## 🎯 Objetivo

Agregar `data-ubits-id="<componentId>"` al elemento root de cada componente en:
- Funciones `render*` (HTML string)
- Funciones `create*` (HTMLElement)

Esto permite que Autorun haga "DOM scan" en Storybook y construya el árbol real de subcomponentes.

